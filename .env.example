# Event-Driven Autonomous Loop — copy to `.env` and fill in. Never commit your real `.env`.
# Every value below is a placeholder; nothing here ties to any account.

CONTINUOUS_LOOP_STATE_PATH=   # Optional — where to persist the queue state. Defaults to ./memory/continuous-loop-state.json.
